# 全局领域词汇

> 状态：已建立  
> 适用范围：跨前端、后端、需求与测试的业务术语；实现、接口与数据库细节仍以对应 SPEC、共享设计和 ADR 为准。

| 术语                               | 统一含义                                                                                    | 不等同于                    |
| ---------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------- |
| 账号（Account）                    | 可登录、可停用、可锁定并拥有会话生命周期的身份记录                                          | 员工档案、角色或菜单项      |
| 用户（User）                       | 管理界面中对账号及其组织归属的业务称呼                                                      | 自动拥有任何权限的主体      |
| 菜单资源（Menu Resource）          | 具有稳定编码的目录、可导航菜单页或不可直接导航的操作资源；是导航和权限资源的注册事实        | 前端写死的侧边栏项          |
| 权限项（Permission Item）          | 服务端可判断、可审计的资源动作与约束组合，包括菜单/操作、数据范围、敏感字段、文件及配置动作 | 仅“是否看到菜单”            |
| 角色（Role）                       | 对一组权限项和适用范围的可追溯授权载体；系统、项目和阶段角色分别定义                        | 项目成员关系或单个用户本身  |
| 数据范围（Data Scope）             | 某项授权可作用的组织、项目、阶段或对象集合及条件                                            | 全局管理员开关              |
| 临时授权（Temporary Grant）        | 具备最小资源范围、原因和起止时间的例外授权，到期自动失效并保留审计                          | 永久特殊角色                |
| 权限决策（Authorization Decision） | 针对指定主体、动作和资源上下文得出的允许/拒绝及解释结果                                     | 前端按钮隐藏逻辑            |
| 初始化系统管理员安全桥接           | IAM-01 期间保护首个本地管理员及管理接口的临时机制                                           | IAM-02 的正式角色或权限实现 |

## 当前已确认的关系

```text
MENU_RESOURCE → PERMISSION_ITEM → ROLE / DATA_SCOPE / TEMPORARY_GRANT
                                      ↓
                       decide(subject, action, resourceContext)
                                      ↓
                   动态导航与服务端业务动作共同使用结果
```

菜单管理与权限管理共同构成独立的 IAM-02 功能域。登录与用户管理只提供账号、组织和当前会话来源；它们不得通过角色下拉框、静态菜单白名单或仅隐藏前端按钮替代上述授权关系。
