# 团队开发协作规范

本规范适用于三名成员分别使用 Trae、Claude Code、Codex，通过公司 GitLab 协作开发本项目。

核心原则是：**一个工作项、一个负责人、一个分支、一个独立工作区、一个合并请求。**

## 1. 协作角色

| 角色 | 责任 |
|---|---|
| 工作项负责人 | 明确范围、完成实现与验证、提交 MR，并对交接信息负责。 |
| 评审人 | 核对事实来源、边界、实现、测试和风险；不能只接受 AI 的总结。 |
| 集成负责人 | 维护共享基础、决定高冲突改动的合并顺序、处理跨工作项冲突。建议按迭代轮换。 |

同一人可以兼任工作项负责人和集成负责人，但自己的 MR 至少由另一名成员评审。

## 2. 开工条件

编码前必须同时满足：

1. 有一个使用 `WI-xxx` 编号的 work-item，并明确唯一负责人。
2. work-item 已关联对应 PRD/SPEC，以及必要的 ADR 或共享设计。
3. 已写清目标、非目标、允许修改的模块或路径、依赖和验收标准。
4. 高冲突区改动已经由集成负责人确认负责人和合并顺序。
5. 每位成员使用独立 Git 克隆或 worktree，AI 只在该工作区内工作。

功能边界尚未收敛时，先完成对应 SPEC 中的功能详细设计；不能让三个 AI 分别从 PRD 猜测实现细节。跨功能复用约束才进入 `docs/technical/designs/`，重大技术取舍进入 ADR。

当前阶段，正式 work-item 和 Bug 记录分别保存在 `docs/development-testing/work-items/` 与 `docs/development-testing/issues/`；GitLab MR 负责代码评审和合并状态。暂不在 GitLab Issue 中复制整份任务正文，避免形成两套状态源；后续若启用 GitLab 看板，应先明确哪些字段只在 GitLab 维护。

## 3. 分支与工作区

默认分支为 `main`，只通过 GitLab Merge Request 合并。

分支命名：

```text
feature/wi-001-short-name
fix/bug-001-short-name
docs/wi-001-short-name
chore/wi-001-short-name
```

规则：

- 每个工作项使用独立短生命周期分支；同一个分支只由一名负责人维护。
- 开工和提交 MR 前同步最新 `main`，冲突由当前分支负责人解决并重新验证。
- 共享分支禁止强推；不得改写、删除或覆盖他人的提交和未提交改动。
- `main` 禁止直接提交。仓库初始化、紧急恢复等例外必须由集成负责人明确授权并记录原因。
- 推荐使用 squash merge；MR 合并后删除来源分支。

## 4. 工作项所有权与冲突区

work-item 的“允许修改范围”是本次任务的所有权边界。AI 发现需要修改边界外文件时：

1. 先说明为什么需要扩大范围以及影响哪些工作项；
2. 更新 work-item；
3. 获得受影响负责人或集成负责人确认；
4. 再继续修改。

以下内容默认属于高冲突区，每次只指定一名修改负责人：

- 根目录工程、依赖和版本配置；
- 认证、授权、账号和审计公共模型；
- OpenAPI、公共 DTO、错误码和跨模块事件；
- 数据库迁移顺序、核心表和公共字段；
- 前端路由、设计令牌和共享组件；
- CI、部署、环境模板和密钥接入；
- `AGENTS.md`、`CONTRIBUTING.md` 和工具规则入口。

## 5. AI 使用规则

- 每次新会话都从仓库事实重新建立上下文，不把 Trae、Claude Code 或 Codex 的聊天记录和自动记忆当成正式结论。
- 个人或全局 AI 规则只补充个人偏好；如与本项目的事实、分支、安全或验证规则冲突，成员负责停用或修订个人规则后再工作。
- 提示 AI 先复述当前 WI、事实来源、范围和完成条件，再开始修改。
- AI 必须保留未知的现有改动；发现文件由他人修改或范围归属不清时，暂停该文件并交给负责人协调。
- AI 生成的代码由工作项负责人承担验证和评审责任。“AI 已检查”不能替代测试证据。
- 本地编辑和检查在当前任务范围内进行；commit、push、创建或合并 MR、部署等动作由成员明确授权。
- 功能设计写入对应 SPEC，跨功能约束写入共享设计，重大技术取舍写入 ADR；不能只留在提示词、聊天记录或工具私有记忆中。

工具入口：

| 工具 | 项目入口 |
|---|---|
| Codex | 根目录 `AGENTS.md` |
| Claude Code | 根目录 `CLAUDE.md` 导入 `AGENTS.md` |
| Trae IDE | `.trae/rules/project_rules.md` 引导读取 `AGENTS.md` |

## 6. 提交与合并请求

提交信息建议使用：

```text
feat(customer): 增加客户创建校验 [WI-012]
fix(auth): 修复失效会话仍可访问 [BUG-006]
docs(spec): 补充文件分享异常流程 [WI-018]
```

每个 MR 必须：

- 关联 work-item 或 Bug；
- 说明关联 SPEC，以及必要的 ADR 或共享设计；
- 列出实际修改范围以及是否超出原范围；
- 说明 API、数据库、配置、安全和兼容性影响；
- 提供最后一次修改后的验证命令与结果；
- 列出未验证项和回退方式；
- 指定至少一名非作者评审人。

MR 使用 `.gitlab/merge_request_templates/Default.md`。流水线和评审未通过时不合并；发生范围或契约争议时，先更新正式事实源再继续评审。

## 7. 数据库与接口协作

- 前后端接口必须有唯一事实源，字段、枚举、分页和错误结构不得由两端分别维护。功能接口设计进入对应 SPEC；跨功能接口规范进入共享设计。当前建议由后端维护 OpenAPI，但需在首个相关 SPEC 或 ADR 中确认后才成为技术基线。
- 已合并的数据库迁移保持不可变；任何调整都创建新迁移。
- 每个迁移使用唯一编号或时间戳，并在 work-item 和 MR 中说明顺序、兼容性、数据校验和回退办法。
- 修改核心实体、公共字段或跨模块关系前，先更新受影响的 SPEC；若属于多个功能共用约束，再同步共享设计或 ADR，并由集成负责人确定合并顺序。

## 8. 验证与完成定义

根目录 README 已提供统一的本地启动和检查入口。当前全量骨架检查使用 `pnpm local:check`；前端单独使用 `pnpm web:check`，后端单独使用 `pnpm backend:test`。每个 MR 至少运行受影响范围的编译、测试和静态检查；登录阶段引入数据库后，涉及迁移的 MR 还必须验证迁移与兼容性。跨端流程要提供端到端或可重复人工测试证据。

当前可运行代码只覆盖本地骨架、组件实验和系统状态接口，不能把这些检查写成登录或业务功能已经通过。

一个工作项只有在以下条件全部满足后才完成：

1. 验收标准逐项有结果；
2. 代码、测试和直接相关文档同步；
3. API、数据库、配置与安全影响已经说明；
4. 必要检查通过，未验证项已明确接受；
5. 至少一名非作者完成评审；
6. MR 合并，work-item 更新最终状态和证据链接。

## 9. GitLab 设置

GitLab 项目创建、成员、主分支保护、审批和后续 CI 设置按 `docs/development-testing/plans/GITLAB_SETUP.md` 执行。平台设置是仓库规则的强制执行层，不能只依赖 AI 自觉遵守。
