# 组织与用户工作区设计 QA

> 范围：本文件只证明 WI-004 交互原型的视觉与浏览器行为；不证明真实 IAM 的认证、授权、接口、持久化、审计或生产级模块化。

## 比较目标与证据

| 场景               | 参考视觉真相                                                                                                               | 实现截图                                           | 同图比较证据                                          | 状态                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------- |
| 组织与用户主工作区 | `/Users/lusice/.codex/generated_images/01a03d53-78ee-7e91-ab48-b3558340e5b2/exec-94a311fa-c0e5-458f-846c-347f64a97dca.png` | `outputs/visual-qa/organization-users-desktop.png` | `outputs/visual-qa/organization-users-comparison.png` | `/system/organization-users` 默认选择“华东业务部” |
| 编辑部门抽屉       | `/Users/lusice/.codex/generated_images/01a03d53-78ee-7e91-ab48-b3558340e5b2/exec-10d1ae72-516d-4ffe-851a-c2435ae7768b.png` | `outputs/visual-qa/department-form-desktop.png`    | `outputs/visual-qa/department-form-comparison.png`    | 编辑当前部门，右侧抽屉打开                        |
| 新建用户抽屉       | `/Users/lusice/.codex/generated_images/01a03d53-78ee-7e91-ab48-b3558340e5b2/exec-b9b9a11c-78f8-4eb3-9246-f1bf13115672.png` | `outputs/visual-qa/new-user-form-desktop.png`      | `outputs/visual-qa/new-user-form-comparison.png`      | 新建用户的初始可编辑状态                          |
| 窄屏工作区         | 无独立移动端参考图                                                                                                         | `outputs/visual-qa/organization-users-mobile.png`  | 不适用；作为响应式核对                                | 390 × 844 CSS px                                  |

桌面主工作区参考与实现均为 **1487 × 1058 px**，在 `1487 × 1058 CSS px`、密度 `1` 下捕获，未缩放。两个表单参考图为 `1486 × 1058 px`，实现为 `1487 × 1058 px`；仅存在 1px 的参考截图边缘差异，比较时左对齐且未重采样。移动端以 `390 × 844 CSS px` 运行；浏览器可截图区域为 `381 × 825 px`（滚动条占用可视区域），未与桌面参考图作失真的像素对比。

表单参考中的具体业务数据分别是已填写/错误状态，而当前实现截图保留真实新建流程的干净初始值。因此表单比较聚焦于抽屉比例、区块层级、标签与控件关系、间距和反馈位置；账号重复错误本身由前端测试覆盖。

## Findings

- [P1，已修复] 桌面目录表在早期渲染时，右侧“最近活跃 / 操作”列被压缩并有裁切风险。
  - 位置：`OrganizationUserWorkspace` 用户目录表。
  - 修复：将数据列压缩为符合参考图密度的固定宽度，并将表格内部滚动宽度设为 `830px`，使卡片边界和操作列始终完整可见。
  - 修复后证据：`outputs/visual-qa/organization-users-desktop.png` 与 `outputs/visual-qa/organization-users-comparison.png`。

- [P2，已修复] 两个表单抽屉早期宽度为 `640px`，比参考图的右侧编辑面板更宽，导致标签—控件比例偏松。
  - 位置：`apps/web/src/features/organization-users/organization-user-workspace.tsx` 的部门与用户 `Drawer`。
  - 修复：调整为 `520px`；普通字段保持左侧固定标签、右侧单一控件，一行只放一个字段；变更说明为标签在上、全宽多行输入。
  - 修复后证据：`outputs/visual-qa/department-form-comparison.png`、`outputs/visual-qa/new-user-form-comparison.png`。

- [P2，已修复] 390px 宽度下，表格的内部最小宽度曾把父级 Grid 推至 `844px`，造成页面级横向溢出。
  - 位置：组织树与用户目录的响应式 Grid。
  - 修复：基础栅格改为 `minmax(0, 1fr)`，把宽表限制在自己的可滚动区域。
  - 修复后证据：在 `390 × 844 CSS px` 下，`document.documentElement.scrollWidth = 381`，不大于 `window.innerWidth = 390`；截图见 `outputs/visual-qa/organization-users-mobile.png`。

- [P2，已修复] 当前组织摘要卡片与下方组织树／用户目录网格在实际渲染中相邻贴合。
  - 原因：`space-y` 的间距被 Ant Design 卡片的默认边距规则覆盖，目录源代码虽声明间距，卡片下方的实际间隔为 `0px`。
  - 修复：工作区改为 `flex` 纵向容器并使用原生 `gap: 16px`。
  - 修复后证据：本地浏览器测量“当前组织”卡片底部至双栏网格顶部为 `16px`。

- [P2，已修复] 新建／编辑部门抽屉的基础信息、状态与排序、变更说明区块之间没有可见的分组间距，且单行控件偏高。
  - 原因：同样依赖可被 Ant Design 表单样式覆盖的 `space-y`；表单控件继承了全局 `40px` 高度。
  - 修复：两个表单改为 `flex` 纵向堆叠并使用 `14px` 区块间距；抽屉内单行输入、选择和树选择控件调整为 `36px`，不影响多行说明。
  - 修复后证据：本地浏览器测量三个部门区块之间均为 `14px`，名称、编码、上级部门与负责人控件均为 `36px`。

- [P2，已修复] 用户目录未提供每页展示条数的选择器，账号状态使用默认 Tag 色阶，停用与锁定的语义区分偏弱。
  - 修复：分页默认展示 `10` 条，并提供 `10 / 20 / 50 条/页`；条数控件采用不可搜索的紧凑下拉选择，使用“每页展示条数”可访问名称，并收回到距卡片右边缘约 `16px`。菜单挂载到页面根节点、固定向上展开，避免底部表格容器裁切或弹出定位抖动；账号状态改为项目内明确的绿色启用、红色停用、琥珀色未激活和深红锁定色阶。
  - 修复后证据：分页器显示“10 条/页”，点击“20 条/页”后状态可更新；浏览器测得分页器与条数选择器距卡片右边缘均为约 `17px`，选择器高度为 `30px`；菜单位于触发器正上方、右边缘对齐，未产生页面滚动；组件测试覆盖点击选择“20 条/页”。

- [P2，已修复] 顶栏全局搜索以 `block` 方式覆盖 Ant Design 输入的原始内联弹性布局，视觉上过高并紧贴顶栏。
  - 修复：改用紧凑 `small` 输入与 `inline-flex` 布局，宽度收为 `220px`，并保留中性边框和聚焦反馈。
  - 修复后证据：本地浏览器测得搜索控件高度为 `30px`，在 58px 顶栏中上、下间距分别约 `13.5px`、`14.5px`。

- [P3，接受] 参考图中的几何品牌标记没有可复用的源资产。实现使用了现有 Lucide 图标库的 `Blocks` 作为中性蓝色工作台标记，没有伪造或复制参考图品牌资产。
  - 影响：不会影响信息架构、交互或可读性；如提供正式 SVG/PNG 品牌资产，应替换该图标以达到完全品牌一致。

## 五项保真面核对

| 保真面            | 结论                                                                                                                                                                                                                   |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 字体与排版        | 使用项目现有 Aptos / Segoe / 苹方回退链。桌面标题、表头、辅助文案与表单标签维持了参考图的紧凑层级；没有异常换行、截断或过重字重。                                                                                      |
| 间距与布局节奏    | 白色侧栏、58px 顶栏、浅蓝灰画布、顶部提示、信息带、组织树与目录双栏，以及 12–14px 圆角白卡按参考层级落地。顶栏全局搜索为 30px；当前组织摘要至主网格为 16px；表单区块间为 14px，普通单行控件为 36px，多行说明独占整行。 |
| 颜色与 Token      | 白色表面、`#f5f7fb` 画布、浅蓝选中/提示、`#2167f3` 主操作与明确的账号状态色阶构成一致的低饱和管理台色彩；启用为绿色、停用为红色、未激活为琥珀色、锁定为深红色。                                                        |
| 图像、Logo 与图标 | 页面没有使用截屏、占位图片、CSS 图形或手写 SVG 替代真实资源。图标均来自现有 Lucide 库且笔画一致；唯一残余为上方 P3 中已说明的正式品牌资产缺口。                                                                        |
| 文案与内容        | 组织、目录、状态、抽屉标题、帮助文字和 Mock 提示均为可独立理解的中文产品文案。参考图与实现的人员数、表单填写值不同，属于受控 Mock 数据差异，不作为视觉漂移。                                                           |

## 交互、可访问性与控制台核对

- 组织树切换到“人力资源部”后，目录结果收敛为 2 条；状态筛选、关键词搜索和选择状态均有可访问名称。
- 点击目录行可打开用户详情，详情中可见“账号信息”；关闭后主上下文保持。
- 编辑当前部门并保存“华东商业部”后，组织树与顶部当前组织信息同步更新；重新加载会恢复受控 Mock 初始数据。
- 新建用户“陈予安 / chen.ya / chen.ya@example.com”后，目录中出现该用户；单测同时覆盖重复登录账号的字段级错误。
- 目录分页默认显示“10 条/页”，可选择 10、20 或 50 条；浏览器已验证点击切换到“20 条/页”。分页条数选择器为 30px 紧凑下拉，距卡片右边缘约 17px，菜单固定在触发器上方展开。
- 顶栏全局搜索是 30px 紧凑输入，在 58px 顶栏中上下居中，保留可访问名称和聚焦边框。
- 390px 下移动导航能打开和关闭（17 个可见菜单项），且页面无横向溢出。
- `pnpm web:check` 已通过：TypeScript、Oxlint、Prettier、Vitest（3 个文件、9 个测试）和 Vite 生产构建均成功。当前环境为 Node `22.23.2`，而仓库声明 `24.19.x`，因此仅保留 package-engine 警告；Vite 另报告既有 Lucide 公共 chunk 大小建议。
- 控制台检查只保留修复前缓存的三条 Drawer `width` 废弃警告和一条 `Descriptions.labelStyle` 废弃警告。当前实现分别改用 `Drawer.size` 与 `Descriptions.styles.label` 后，在重新加载、打开并关闭当前用户详情抽屉的验证中 warn/error 记录数保持 `4 → 4`，没有新增记录。

## Open Questions

- 如需要品牌级像素一致，请提供“项目工作台”的正式 Logo 资产及其最小尺寸/浅色背景用法。当前中性图标满足该前端切片的结构和可读性，不应被当作最终品牌资产。
- 当前 `outputs/visual-qa` 的桌面截图记录的是本轮反馈前的版本；在创建提交或 MR 前，应以相同桌面视口重新捕获主工作区和部门抽屉截图，使像素证据与当前间距、状态色和分页规格一致。

## Implementation Checklist

- [x] 以同一桌面视口将主工作区、部门抽屉与用户抽屉的参考图和浏览器截图放入同一比较图。
- [x] 修复桌面表格列裁切、表单抽屉比例与移动端页面级横向溢出。
- [x] 修复当前组织摘要到主网格、部门表单区块间距、抽屉控件高度、账号状态色和分页条数选择器。
- [x] 核对字体、空间、色彩、图标/资源与应用文案五项保真面。
- [x] 验证组织筛选、详情、部门保存、新建用户、表单校验、移动导航与控制台状态。

## Follow-up Polish

- 在获得正式 Logo 后替换中性 `Blocks` 标记，并对 1× / 2× 资源边缘做一次品牌验收。

final result: prototype-local behavior passed; refreshed desktop screenshot evidence pending before commit or MR. This is not formal IAM implementation acceptance.

---

# 菜单资源注册台（方案 2）设计 QA

> 范围：仅验证 `/component-lab?scene=menu-resources` 的开发期视觉交互原型。它不接入生产导航、真实鉴权、菜单 API、持久化或审计链路，不能作为 WI-007 的正式 IAM 验收。

## 比较目标与证据

| 项目         | 证据                                                                                                                       |
| ------------ | -------------------------------------------------------------------------------------------------------------------------- |
| 参考视觉真相 | `/Users/lusice/.codex/generated_images/01a04661-4a4d-7d32-b181-9820a03d86ab/exec-a834aa43-c11d-4b21-92ea-8410cf7bcbe9.png` |
| 实现截图     | `outputs/visual-qa/menu-resource-registry-rendered-1487x1058.png`                                                          |
| 原型入口     | `http://127.0.0.1:5173/component-lab?scene=menu-resources`                                                                 |
| 目标状态     | 默认选中“项目列表”；资源树展开；资源表显示 12 条；详情面板显示“启用”与停用影响预警。                                       |
| 视口与密度   | 参考图和实现截图均为 `1487 × 1058 px`；实现用 `1487 × 1058 CSS px`、`deviceScaleFactor 1` 捕获，未重采样。                 |

## Findings

- [P1，验证阻塞] 浏览器安全策略阻止打开本地 `file:` 比较画布，因此参考图与实现截图无法被放入**同一比较输入**。
  - 位置：`outputs/visual-qa/menu-resource-registry-comparison.html` 的本地比较画布。
  - 证据：已分别打开参考图和同尺寸实现截图；应用内浏览器拒绝导航到该本地比较页。
  - 影响：按照 Product Design 的交付门，不能把两张独立查看的图片表述为正式设计 QA 通过。
  - 修复：等待能够安全显示同图对照的批准方式或可访问的比较媒介；在此之前保留此原型为“已实现、视觉 QA 阻塞”。

- [P3，接受] 参考图的几何产品标记没有提供可复用源资产；原型沿用项目已安装的 Lucide `Blocks` 图标作为中性工作台标记。
  - 影响：不改变信息架构、操作路径或可读性；获得正式 Logo 后应替换。

## 五项保真面核对（独立截图复核，非同图通过）

| 保真面            | 观察结果                                                                                                                                                                 |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 字体与排版        | 使用项目既有 Inter / Segoe / 苹方回退链；顶栏、树、紧凑表格和右侧表单均维持 12–15px 的后台信息层级，未观察到默认状态下的异常换行或表格列裁切。                           |
| 间距与布局节奏    | 按参考图复现约 `242px` 左导航、`62px` 顶栏、`260px / 自适应 / 338px` 三栏、顶部信息提示与 `1020px` 左右的卡片底线；详情字段采用标签—控件同排，使状态与停用预警处于首屏。 |
| 颜色与 Token      | 使用现有项目 `#f5f7fb` 画布、白色面板、`#3157d5` 主操作、浅蓝选中行和低饱和绿色状态，停用预警使用琥珀描边和低饱和背景。                                                  |
| 图像、Logo 与图标 | 参考图没有照片、插画或其他栅格内容资产；界面图标全部来自现有 Lucide 库，没有使用截屏、CSS 图形或手写 SVG 替代资源。正式 Logo 是唯一已知资产缺口，见上方 P3。             |
| 文案与内容        | 所有页面文案、受控资源编码、routeKey 帮助和影响预警均为独立可理解的中文后台管理文案；数据仅存在于 Component Lab 的内存状态。                                             |

## 浏览器交互、可访问性与控制台核对

- 点击“项目详情”行后，资源树、表格选择态和详情编辑值同步为 `项目详情 / menu.project.detail`。
- 在资源表关键词输入“导出”后，保留“导出项目”且“项目详情”行被过滤。
- 选择“停用”后，单选状态变为选中；保存后显示“项目列表 已保存（仅原型状态）”。
- 所有核心输入均有可访问名称；树、表格行、单选状态和保存按钮可由语义定位。
- 最终重载后的浏览器控制台 `warn/error` 记录为 `[]`。

## 比较历史

1. **初次独立渲染复核**：顶栏账号区有换行风险，资源树行距偏松，资源表操作区的搜索框会折至第二行，详情字段采用纵向堆叠导致状态与预警不在首屏。
2. **修复**：固定顶栏搜索的 `236px` 宽度并禁止账号收缩；压紧资源树行距；收窄表格列与单元格内边距、让操作区保持单行；详情字段改为标签—控件同排，预警压缩为参考图的紧凑高度。
3. **修复后证据**：`outputs/visual-qa/menu-resource-registry-rendered-1487x1058.png` 显示三栏底线、资源表 12 条、状态和完整影响预警均处于首屏。尝试创建同图比较证据时受上述 P1 安全策略阻塞，故本记录不宣称正式视觉对照通过。

## 人工验收结论

- **2026-08-28：用户确认开发期原型验收通过。**
- 该结论只覆盖 Component Lab 中的菜单资源注册台视觉与核心交互；不构成 WI-007 的生产 IAM、菜单动态加载、服务端鉴权、审计或持久化验收。
- P1 的同图比较限制继续保留为审计记录：它不再阻塞此次人工原型验收，但在可用的安全比较媒介出现前，自动化设计 QA 仍不能表述为通过。

## Open Questions

- 需要一个可被当前浏览器安全策略打开的同图对照媒介，才能把参考图与实现截图放入同一输入并关闭 P1 验证阻塞。

## Implementation Checklist

- [x] 在既有开发期 Component Lab 中隔离实现方案 2，不改变生产路由或 IAM 运行时。
- [x] 实现资源树、筛选资源表、详情编辑、状态切换、停用影响预警和内存态保存反馈。
- [x] 在 `1487 × 1058` 桌面视口下捕获实现截图，并验证核心浏览器交互与控制台。
- [x] 用户已确认开发期视觉与核心交互原型验收通过；该确认不关闭 P1 自动设计 QA 阻塞。
- [ ] 将参考图和实现截图置于同一可比较输入，关闭 P1 设计 QA 阻塞。

## Follow-up Polish

- 提供正式 Logo 后替换中性 Lucide 标记；同图比较可用后，再决定是否需要微调树图标与品牌标记的像素级一致性。

final result: blocked

manual acceptance: passed — development-only prototype; P1 audit exception remains

---

# 菜单资源注册台（重建版）设计 QA — 2026-09-02

> 范围：仅验证 `/component-lab?scene=menu-resources` 的开发期 Component Lab 原型。它不接入真实菜单 API、授权决策、持久化或审计，**不构成 WI-007 正式 IAM 验收通过**。

## 比较目标与证据

| 场景             | 参考视觉真相                                                                                                        | 规范化参考图                                                                        | 实现截图                                                                        | 视口、状态与密度规范化                                                                                                                                                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 菜单资源列表     | `/private/var/folders/7v/v0ftxp290nv6727b7xz0xg7c0000gn/T/codex-clipboard-a0e3dd6b-e67d-4eea-b793-5269d477a41c.png` | `outputs/visual-qa/menu-resource-reference-list-normalized-2026-09-02-1281x861.png` | `outputs/visual-qa/menu-resource-registry-list-final-2026-09-02-1290x867.png`   | 参考为 `2580 × 1734 px`；按 2× 像素密度下采样为 `1281 × 861 px`。实现运行于 `1290 × 867 CSS px`、DPR 1；浏览器截图排除了滚动条区域，实际为 `1281 × 861 px`。默认选中“工作台”，页面滚动 `72px` 以匹配参考图从工作台 Tab 开始的可视状态。 |
| 编辑菜单资源抽屉 | `/private/var/folders/7v/v0ftxp290nv6727b7xz0xg7c0000gn/T/codex-clipboard-99daec1f-2f2f-4030-b0bd-fd71a4ea942f.png` | `outputs/visual-qa/menu-resource-reference-drawer-2026-09-02-1500x938.png`          | `outputs/visual-qa/menu-resource-registry-drawer-final-2026-09-02-1500x938.png` | 参考为 `3000 × 1876 px`；按 2× 像素密度下采样为 `1500 × 938 px`。实现运行于同尺寸 CSS 视口、DPR 1；默认选中“工作台”并打开“编辑菜单资源”抽屉。                                                                                           |

两组规范化参考图与相应实现截图均在同一次视觉比较输入中打开并复核；没有根据代码或记忆代替屏幕比较。列表的重点区域为左侧资源树与表格起始线，抽屉的重点区域为右侧宽度、表单分组和“受控 routeKey / 动作键”字段，因此进行了全图与重点区域两层核对。

## Findings 与比较历史

1. **初次重建复核：P1，已修复 — 外层导航与树表网格横向错位。**
   - 证据：初始实现侧栏宽 `204px`，而完整抽屉参考图归一化后的侧栏为约 `180px`；资源树与表格因此整体右移，树表间距也不一致。
   - 修复：将 Component Lab 专用侧栏及主区偏移统一为 `180px`，资源树保持 `260px`，树表间距调整为 `20px`。
   - 修复后测量（`1500 × 938`）：侧栏 `x=0 / width=180`、资源树 `x=200 / width=260`、资源表 `x=480`、抽屉 `x=980 / width=520`；与归一化后的参考主要几何对齐。

2. **初次重建复核：P2，已修复 — 受控键的已选文案夹带资源名称。**
   - 证据：实现显示 `system.overview（工作台）`，参考抽屉显示稳定键 `system.overview`。
   - 修复：下拉项与已选态均只显示稳定 `routeKey`；下拉仍为可操作的受控选择。
   - 修复后证据：`menu-resource-registry-drawer-final-2026-09-02-1500x938.png` 中的已选值为 `system.overview`。

3. **P3，接受 — 两张原始参考图都带有顶部裁切。**
   - 参考图在最上方约一个小型标题行处已被截断，不能据此反推页面或抽屉的实际上边距。该差异不影响选中态、栅格、抽屉宽度、字段层级或核心操作，故不以此对实现做过拟合调整。

## 五项保真面核对

| 保真面            | 结论                                                                                                                                                                                  |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 字体与排版        | 沿用项目既有 Inter / Segoe / 苹方回退链。Tab、表头、树节点、状态标签和抽屉字段保持参考图的紧凑后台层级；没有异常换行或字段标签截断。                                                  |
| 间距与布局节奏    | 修复后侧栏、资源树、表格与 520px 抽屉的关键横向几何一致；卡片圆角、边线、紧凑行高、选择带及抽屉内两段表单卡片与参考图同一层级。顶部裁切仅作为参考捕获差异记录，不转化为产品布局改动。 |
| 颜色与 Token      | 使用既有浅蓝选中态、白色面板、低饱和灰蓝文字、绿色启用标签及 `#2f68df` 主操作；未引入与参考冲突的新色阶。                                                                             |
| 图像、Logo 与图标 | 参考页没有需要补制的栅格资产；图标均复用项目已安装的 Lucide 图标，没有用截图、CSS 绘图或手写 SVG 伪造可见资产。正式品牌标记仍非本轮范围。                                             |
| 文案与内容        | “工作台”、资源编码、`routeKey`、状态、后续流程与动作标签均为可独立理解的中文后台管理文案；抽屉中的已选受控键已与参考稳定键表达一致。                                                  |

## 浏览器交互、可访问性与控制台核对

- “安全策略”关键词将表格收敛到 `security.policy.config`，并隐藏“系统概览”。
- 状态下拉选择“状态：停用”后，表格结果为 0 条，证明筛选实际生效。
- 新建抽屉的“资源类型”下拉可打开并选择“操作资源”。
- 输入“浏览器验收资源 / BROWSER_QA_RESOURCE”后保存，出现“已保存（仅原型状态）”反馈且资源出现在表格；刷新后该资源消失，证明状态只驻留在本原型内存中。
- 编辑“工作台”可打开 520px 右侧抽屉；受控键、状态等控件均保持可访问名称。
- 最后一次浏览器复核的 `warn/error` 控制台记录为空。

## 实施与验证清单

- [x] 以同尺寸、同状态的参考图和实现截图进行全图与重点区域比较。
- [x] 修复外层导航、资源树、资源表和抽屉的 P1 横向几何偏差。
- [x] 修复受控键已选文案的 P2 偏差。
- [x] 浏览器验证搜索、状态筛选、类型下拉、编辑、新建、保存反馈、刷新复位和控制台。
- [x] `pnpm --dir apps/web typecheck`、`lint`、受影响 Vitest（11 文件 / 22 测试）、目标文件 Prettier 检查与生产构建均通过；构建仅保留既有 Vite 大 chunk 提示。

## Follow-up Polish

- 若后续提供未裁切的原始参考图，可仅针对顶部标题区再做一次像素级捕获校对；当前无可操作的 P0/P1/P2 偏差。

final result: passed

---

# 正式 IAM 工作区视觉迁移与精简 QA — 2026-09-02

> 范围：将已确认的“菜单资源注册台（重建版）”视觉语言落实到正式 IAM 工作区，并针对真实桌面页的“过大、臃肿、抽屉标签在上”、资源树横向滚动和受控能力字段脱离栅格问题做第二轮精简；不变更 API、授权决策、持久化、审计或业务字段语义。

## 比较目标与当前证据

| 项目             | 证据                                                                                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 视觉真相         | `outputs/visual-qa/menu-resource-reference-list-normalized-2026-09-02-1281x861.png` 与 `outputs/visual-qa/menu-resource-reference-drawer-2026-09-02-1500x938.png`。 |
| 已迁移范围       | 角色与矩阵、菜单资源、权限项目录、系统角色授权、临时授权、有效权限预览，以及共用 IAM 表单抽屉。                                                                     |
| 已验证的真实页面 | 已认证本地会话中的角色与矩阵、菜单资源和临时授权；已以非写入方式打开角色编辑、菜单资源编辑和临时授权新建抽屉。                                                      |
| 当前桌面窗口     | `901 × 969 CSS px`，DPR `1`；用于检查真实数据密度、工具栏换行和抽屉表单比例。                                                                                       |
| 已实现的视觉规则 | `12px` 面板圆角、`36px` 工具栏与表单控件、`48px/60px` 表头/数据行、紧凑选择带，以及标准抽屉的左标签／右控件结构。                                                   |

## Findings

- [P2，已修复] 正式角色页的标题、Tab 带、目录、工具栏、表格行和摘要卡此前留白过大，首屏信息密度不足。
  - 处理：收紧间距、面板圆角、表头与行高、左侧角色目录、工具栏与主操作尺寸；角色工具栏在当前窗口不再让筛选按钮孤立换行。
- [P2，已修复] 表单抽屉此前为标签在上、控件在下的纵向堆叠，且宽度、分组和底部操作占用过大。
  - 处理：角色、菜单资源、系统角色授权、临时授权与撤销抽屉均采用左侧标签／右侧控件。说明与多行输入只将标签顶对齐。
- [P1，已修复] 角色“权限矩阵”中的授权条目将两列网格与外层横向 Form 的固定标签列叠加，权限控件的实际可用宽度被压成 `0px`，导致标签、控件和右侧边界重叠。
  - 处理：授权条目改为单列列表；每项使用独立 `96px` 左标签和可伸缩右控件。真实浏览器复核中，首个权限控件宽度由 `0px` 恢复为 `323px`。
- [P2，已修复] 资源树被强制设置为 `600px` 最小高度，即使只有少量资源也会留下整块空白。
  - 处理：资源树改为内容自适应，长目录才在树体内部滚动；窄屏时栅格保持单列且页面无水平溢出。
- [P1，已修复] 资源树的滚动容器此前同时允许横向滚动，节点标题也没有形成可收缩边界，因此在窄侧栏中出现了无意义的底部横向滚动条。
  - 处理：树体只保留纵向滚动；树列表、节点和标题统一设为可收缩宽度，长名称以省略号显示，并用原始名称作为悬停提示。
- [P2，已修复] 菜单资源编辑抽屉中的“图标键”此前藏在脱离 Form 栅格的“补充图标键”折叠入口中，排序输入框也没有随右侧控件列拉满。
  - 处理：图标键改为“受控能力”内的普通可选字段；路由／动作键、图标键、排序和状态均使用同一条左标签／右侧全宽控件结构。
- [P2，已修复] 资源分类树的 `240px` 桌面列宽不足以舒适阅读多层资源名称；同时资源表中的路由键列占用大量横向空间，却与编辑时的受控键选择重复。
  - 处理：树列扩至 `300px`；资源表移除“路由键（routeKey）”列，并将表格最小横向宽度由 `1090px` 收紧至 `900px`。搜索和编辑中的受控键选择保持不变。
- [P2，已修复] 目录型表格此前默认每页 `20` 条；用户切换为更大页码后，表格随内容增长并把整页拉长。
  - 处理：共享分页默认改为 `10` 条／页；所有正式目录表都使用同一可视区高度，表头和分页固定在表格外缘，超出行仅在表格主体内纵向滚动并在分页变化后回到表格顶端。
- 当前真实会话中的浏览器日志没有 `warn` 或 `error`；未执行任何保存、创建、撤销或其他浏览器写入。

## 已完成的迁移与自动化回归

- 角色与矩阵及菜单资源保留现有查询、选择、筛选、保存与影响提示，只收敛视觉比例和信息层级。
- 所有标准 IAM 抽屉统一至约 `520px` 宽（撤销确认 `440px`），页头、表单分组、控件和页脚一起收紧；角色授权条目不再继承外层表单的固定标签宽度。
- 权限项目录、系统角色授权和临时授权继承同一目录型页头与紧凑工具栏；有效权限预览同步减少不必要的固定高度与空白。
- `pnpm --dir apps/web typecheck`、`lint`、受影响 Vitest（11 文件 / 23 测试）、目标文件 Prettier 与生产构建均通过；新增交互回归断言确认图标键是常规字段、排序仍可访问、不再存在“补充图标键”折叠入口，且表格不再渲染路由键列表头。共享分页测试覆盖默认 `10` 条／页与固定表格视口。构建仅保留既有 Vite 大 chunk 提示。
- 本地 Spring Boot 健康检查为 `UP`；正式浏览器验收均为非写入操作。

## 五项保真面

| 保真面            | 结论                                                                                                  |
| ----------------- | ----------------------------------------------------------------------------------------------------- |
| 字体与排版        | 标题、Tab、表头、辅助文案与抽屉标题使用同一后台层级；修复后角色授权控件完整可见，工具栏不再异常换行。 |
| 间距与布局节奏    | 页面与抽屉的行距、内边距、控件高度均已收紧；资源树不再被少量内容强制撑成整块空白。                    |
| 颜色与 Token      | 保持白色面板、浅蓝选择态、低饱和灰蓝文本、绿色状态和 `#2f68df` 主操作色，不改变业务状态含义。         |
| 图像、Logo 与图标 | 不新增栅格素材；继续使用项目已有 Lucide 图标。                                                        |
| 文案与内容        | 真实字段、校验、操作命名和审计提示保持不变，仅调整信息承载方式。                                      |

## Implementation Checklist

- [x] 将确认的资源目录桌面规则迁移到正式 IAM 页面与共享表单抽屉。
- [x] 保持 API、授权、审计、持久化和字段校验不变。
- [x] 以已认证的真实会话检查角色主工作区、菜单资源主工作区，以及角色、资源、临时授权抽屉。
- [x] 验证抽屉普通字段均为左标签／右控件，工具栏在当前桌面宽度不产生孤立换行。
- [x] 验证角色授权条目的控件宽度不为零，菜单资源树只纵向滚动并能截断长标题；菜单资源在当前窄窗口下为单列且没有页面级横向溢出。
- [x] 验证菜单页和操作资源的“受控能力”均为左标签／右侧等宽控件，不存在游离的折叠字段或窄排序框。
- [x] 验证资源树在桌面使用更宽的 `300px` 阅读列，资源表不再展示路由键／动作键明细列。
- [x] 验证所有正式目录表的默认分页为 `10` 条，选择更多条目后只滚动表格主体。
- [x] 通过前端静态检查、受影响交互测试、生产构建、本地后端健康检查和浏览器日志检查。

## Follow-up Polish

- 如后续需要覆盖超宽屏或移动端，只针对断点下的列宽和标签折行做增量核对，不回退当前桌面密度。

final result: passed
