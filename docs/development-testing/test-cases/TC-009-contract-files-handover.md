# TC-009：合同、文件与交付移交

来源 WI-009、SPEC-CT-01 v0.2、SPEC-DOC-01、SPEC-HG-01、SPEC-HG-02。2026-09-13 本地执行通过。

| 范围 | 预期及已执行验证 |
| --- | --- |
| 主线与权限 | 原项目派生客户/商机；编号唯一、重复提交同结果；归档角色不能编辑；普通成员看摘要且无金额；非成员 404 |
| 实际文件 | 真实 S3 协议上传、SHA-256 与下载一致；查看不包含下载权；合同密级限制；草稿不能作为签署依据 |
| 版本 | 独立发布；修订草稿保留原有效版；新版发布使旧引用失效；历史仍可下载；同键不同内容 409 |
| 归档 | 提交后文件变化阻断接收；退回、重提、独立接收；主阶段不切换；补充协议保留前后金额及资料；自审拒绝 |
| 节点 | 合计不超过合同金额；完成日期和依据；重复完成拒绝；完成不产生实际资金流水 |
| 节点更正 | 原因必填；归档后约定调整须签署变更依据；完成纠正/误报撤销保留前后事实；旧版本与不同负载重放拒绝；超额整笔回滚；归档中冻结节点 |
| DG-01 | 真实清单、条件适用、缺项与受限源；源变化使快照过期；独立接收、退回重提、历史包不可变，主阶段仍为售前 |
| 提前开工 | 独立批准及范围/期限/费用/工时控制；并发承诺不越限；部分实际不重复占用；冲回顺序、超限说明及冻结、停止、收口授权、正常转正；未生效授权拒绝新增承诺 |
| 失败恢复 | 存储离线返回 503 且不保存虚假元数据；恢复服务后原签署件和浏览器文件校验一致；空文件、路径文件名 400、超限 413 |
| 浏览器 | 营销真实选择并上传附件、提交评审；归档账号独立发布；营销关联技术附件并提交；归档账号独立接收；刷新持久化；只读节点无保存按钮 |
| 视觉 | draw-ui 原图与 1505 × 1045 实际截图、7 个采集锚点；390px 页面不横向溢出，表格内部滚动 |
| 迁移与构建 | 空库 V1–V8 启动及 V9 升级、既有节点基线保留；后端 20 测试、前端 26 测试及类型/Lint/格式/构建通过 |

自动化：`scripts/development/contract-flow-check.mjs`（9 组）、`contract-storage-check.mjs`（离线 1 组、恢复 4 组）。合成证据：`outputs/verification/wi-009/`；截图及校准：`outputs/design/wi-009/`。全部账号、公司、合同和文件为合成验证数据，不构成真实签署。

追加自动化：`contract-node-check.mjs`（5 组，`outputs/verification/wi-009-nodes/`）、`handover-flow-check.mjs`（7 组，`outputs/verification/wi-009-handover/`）。浏览器已完成移交缺项拦截/补齐/提交/独立通过，先行投入申请/独立批准/承诺/部分实际和节点更正/历史回读。金额必填曾误按字符串验证，已修复并增加 2 个组件回归用例；只读 Directory 预期拒绝曾触发外层事务 500，已修复并经受限账号实际请求回归。

尚未执行：生产存储授权/备份/容量/扫描/分享、真实公司角色及模板制度验收。DG-02、完整交付和财务等不在本用例已通过结论内。
