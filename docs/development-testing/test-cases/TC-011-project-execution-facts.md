# TC-011：原批准阶段、清单与里程碑执行

> WI-011 / SPEC-EXEC-01；负责人 Lusice / 实施与验证 Codex；2026-09-13；本地验证通过。

| 分组 | 验收与结果 | 证据 |
|---|---|---|
| 原对象与准入 | 原项目 V4 的四个阶段、清单、里程碑、工作包 ID 不变；实际另存；原 DONE 不补造开始日期；未开始工作包的完成动作隐藏且命令拒绝 | `outputs/verification/wi-011-execution/before.json`、`preflight.json` |
| 阶段控制 | 实际经理开始、暂停、恢复、完成、重开；前置未完成拒绝开始；报告进度与完成分离；有缺项不能完成 | `quantity-checks.json`、`finish-checks.json`；阶段服务集成 |
| 分批数量 | 合成设备批准 8 套，当前净到货 8、净安装 4、已验收 2、待核验 1；待验收占余量，不以安装比例冒充验收比例 | `final-equipment.json`；数量集成与前端展示测试 |
| 并发与幂等 | 同一版本并发录入两笔到货只有一笔成功；另一笔 409；以新版本超量也拒绝；相同请求重放不新增事实 | `concurrent-input.json`、`quantity-checks.json`；真实 PostgreSQL HTTP |
| 更正 | 有下游数量时不能冲回到货；整条冲回保留原数量，追加反向记录；重放冲回仅一次；新正确记录另记 | 分批服务集成；真实第二批到货冲回、补录与读回 |
| 实际日期 | 不能填未来，不能早于阶段开始；同一天内累计及跨日期前缀数量守恒；完成日期不得早于已记事实 | 集成；`facts-prepared.json` 的日期、超量与拒绝后无新增记录 |
| 独立验收 | 当前负责人提交，指定其他启用成员验证；提交人无自验权；仅指定人的队列展示该批；撤权即时生效 | 多账号浏览器提交与核验；`quantity-checks.json`；权限服务集成 |
| 里程碑与阶段 | 当前 Work DONE、关联清单全验收后可提交；独立里程碑核验前不能完成阶段；已完成下游须先明确重开 | `finish-checks.json`；浏览器经理提交、指定验证人确认 |
| 两轮与附件 | 明确重开后原轮不覆盖；五条里程碑历史包含两次提交、两次验证与一次重开；每轮原文件版本保留 | `milestone-history.json`；历史文件版本变更/撤权集成测试 |
| 当前范围变化 | 不允许停用有有效实际的对象、缩小到净实际以下或改换已执行清单关系；实质依据变化传递至关联里程碑/阶段 NEEDS_REVIEW | 真实执行存储与受控来源替身集成；不宣称已进行额外公司实质范围审批试点 |
| 责任交接 | 待核验人员不能直接移除；接任不可兼任当前处理者，需有效读取/核验权限；交接保留提交者，旧预览不得重放 | 第六项执行集成测试覆盖清单指定核验交接；原人员交接总流程由 WI-010 验证 |
| 文件与隔离 | 源文件提交/通过重新鉴权；历史原版本保存在事件中，受限文件只返回数量，JSON 不泄露隐藏 ID/标题；外部与已离组人员返回 404 | 执行文件测试、`preflight.json`、`restart-readback.json` |
| 检索与窄屏 | 阶段名称/责任与状态、清单类别/阶段/工作包/完成状态、里程碑计划/责任/状态、分批类型/状态/日期；筛选折叠；手机先展示本人待核验，表格局部横滚 | 多账号桌面/390px 浏览器与 `mobile-item-filter.png`；边界日期和验收筛选前端用例 |
| 迁移与持久性 | 精确新库零张 public 表，一次应用 V1–V18 后 JPA 验证及健康检查通过；主库重启后原基线、完整实际和两轮附件读回相等 | `restart-readback.json` |

自动检查：后端全量 55 项零失败/错误/跳过，含 6 项执行集成；前端全量 40 项 / 15 文件，加类型、Lint、格式与生产构建通过。日志分别为 `.local-data/wi011-backend-full-v3.log` 和 `.local-data/wi011-web-final.log`。后台集成的外部领域部分使用受控替身，真实 PostgreSQL、私有 S3 原版本和多账号浏览器另有证据，二者不互相替代。

设计图、实际浏览器截图与 draw-ui 测量分开保存。校准第一轮发现侧栏类名连接错误，已修复；第二轮因仍停留里程碑页而被身份检查拒绝，保留失败报告。最终必须以正确项目阶段 URL 和重新采集结果验收，不修改旧元数据。浏览器旧会话过期 401、开发重载记录及自动化定位超时不当作业务失败；正常新页面需重新检查控制台。构建约 618 kB 的公共依赖块仍待整体性能验收。

commit/push/MR、公司试点、正式验收模板、采购分包与经营财务、统一消息、任务周计划/工时、报告例会及生产部署不属于本批完成结论，继续作为整个系统目标推进。

最终校准第 3 轮七区域及正确页面身份通过，实际查看桌面、手机和只读历史，无页面横向溢出。最后仅收紧清单抽屉表格行距与内边距，构建和手机截图 `mobile-item-summary-v2.png` 再次通过；[ACCEPT-011](../accept/ACCEPT-011-project-execution-local.md) 记录本批结论。
