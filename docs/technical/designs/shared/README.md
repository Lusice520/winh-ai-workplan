# 跨端共享设计

本目录存放需要前端和后端共同遵守的共享契约，例如身份与权限、文件分享、流程状态、审计、规则和跨模块事件。

单功能实现仍全部进入对应 SPEC；只有两个以上功能共同依赖的稳定契约才进入本目录。共享设计不得替代 BRD、PRD 或 SPEC，也不得自行新增产品状态和用户可见规则。

当前已基线的契约：

- [IAM Foundation Contract](./IAM_FOUNDATION_CONTRACT.md)：登录、会话、CSRF、问题响应、分页、审计与后续权限替换边界。
