# 执行事实验证记录

2026-09-13。本地项目 `c2ebb10c-39e1-4cdb-bca0-00724ffa808e` 是明确标记的合成数据；无真实客户资料、密码、Cookie 或对象存储密钥写入本目录。

- 后端全套 55 项通过：`.local-data/wi011-backend-full-v3.log`；6 项执行集成包括分批完成、顺序和冲回、范围失效、命令时权限/文件、历史附件、指定核验交接。
- 前端全套 40 项、15 文件，类型/Lint/格式/构建通过：`.local-data/wi011-web-final.log`；最后仅收紧执行抽屉表格密度，`.local-data/wi011-final-drawer-build.log` 构建通过并查看手机截图。
- 真实 API 使用本目录 fixture 及 `scripts/development/execution-*.mjs`：先预检，经理浏览器开始阶段，再创建受控分批事实、负责人浏览器提交、并发/幂等检查、其他账号浏览器核验，最后完成与重开两轮。状态已推进，不要重复运行 preflight 或 quantity-checks；`execution-readback.mjs` 为可重复只读验证。
- `execution-fixture-actions.mjs` 在写入前保存命令原负载与请求 ID；只针对本次精确合成项目，不负责重置数据。真实并发为同一原版本两笔到货，结果一笔成功、一笔 409，重新用最新版本超量仍拒绝。
- 主库最终服务重启后，Workspace、清单明细、里程碑五条原历史与重启前深度比较一致；独立队列仍只含第三套设备的待核验。原批准对象与先前 DONE 工作包相等。
- 新库 `winh_wi011_execution_clean_20260913` 启动前 public 表数为 0；V1–V18 一次迁移、JPA 校验与健康检查通过。主库、新库各 18 条成功迁移；主服务 8080，新库验证服务 8082。
- 正常最终页面只出现 React 开发提示。旧日志含过期会话 401、故意负例与重载；新增执行页的 Ant Design `Space.direction` 兼容提示已改用 `orientation`。自动化因虚拟选项和历史弹层定位发生的超时保留为过程记录，后续使用真实可见选项与同次导航/展开完成验证。
- draw-ui 第 1 轮发现侧栏类名错误并修复；第 2 轮页面停在里程碑，身份校验拒绝；第 3 轮正确路由、1522×1033 / DPR 1 / CSS 视口截图、7 个区域、字体/图片就绪与无横向溢出均通过。失败报告不覆盖。几何差异来自固定项目导航、真实长名称/条件、主列宽度与详情分区，记录在 round-notes 中，不以采集通过替代视觉审阅。

最终页面、手机队列/清单、独立核验表单、里程碑前后历史均实际打开查看。完整记录最多展示项目最近 100 条，原对象历史可继续完整查询；只读历史无保存按钮。未实现的后续模块不计入本次完成。
